package model.cards;

/** The different possible tiers of a {@link Card}. */
public enum Tier {
    /** The {@link Card} is in the first tier (1 dot). */
    LOW,
    /** The {@link Card} is in the second tier (2 dots). */
    MID,
    /** The {@link Card} is in the third tier (3 dots). */
    HIGH
}
