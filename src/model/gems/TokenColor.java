package model.gems;

/** The different possible colors of a token. */
public enum TokenColor {
    /** The token is brown. */
    BROWN,
    /** The token is red. */
    RED,
    /** The token is green. */
    GREEN,
    /** The token is blue. */
    BLUE,
    /** The token is white. */
    WHITE,
    /** The token is gold. */
    GOLD
}
